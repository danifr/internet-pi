# {{ ansible_managed }}
---
services:
  starlink-exporter:
    container_name: starlink-exporter
    image: danopstech/starlink_exporter:{{ docker_image_versions.starlink_exporter }}
    ports:
      - "9817:9817"
    restart: unless-stopped
